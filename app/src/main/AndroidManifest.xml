<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.frx.jitepaikejava">

    <application
        android:name="com.frx.jitepaikejava.DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplitEditDemo.SplitEditDemoActivity" />
        <activity android:name=".NoIfElseDemo.NoIfActivity" />
        <activity android:name=".StatusLayoutDemo.StatusLayoutActivity" />
    </application>

</manifest>